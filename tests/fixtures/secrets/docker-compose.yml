version: "3.8"

# Correlation test fixture — DO NOT use in production
# DB_PASSWORD chains from .env → here → app.py

services:
  app:
    image: myapp:latest
    environment:
      - DB_PASSWORD=${DB_PASSWORD}
      - STRIPE_KEY=sk_live_abcdefghijklmnopqrstuvwxyz123456
      - GITHUB_TOKEN=ghp_aBcDeFgHiJkLmNoPqRsTuVwXyZ123456789012
    ports:
      - "8080:8080"

  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: super_secret_password_123
      POSTGRES_USER: admin
